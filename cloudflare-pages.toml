# Cloudflare Pages 専用設定
# Workersは別途デプロイしてください

# ビルド設定
[build]
  command = "npm run build"
  publish = "dist"

# 環境変数
[build.environment]
  NODE_VERSION = "18"

# リダイレクト設定（SPA用）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Workersの設定は含めない
# Workerは workers/ ディレクトリから別途デプロイ
